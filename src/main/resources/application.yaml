spring:
  application:
    name: demo

  cache:
    cache-names: cache
    type: redis

  session:
    redis:
      namespace: session-namespace
      flush-mode: on_save

server:
  servlet:
    session:
      cookie:
        name: JSESSIONID


